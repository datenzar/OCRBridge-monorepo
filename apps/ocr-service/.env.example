# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_WORKERS=4

# File Storage
UPLOAD_DIR=/tmp/uploads
RESULTS_DIR=/tmp/results
MAX_UPLOAD_SIZE_MB=25

# Job Configuration
JOB_EXPIRATION_HOURS=48

# OCR Configuration
# ==================
# Multiple OCR engine flavors are supported:
#   - tesseract: Lightweight, CPU-only OCR (always available)
#   - easyocr: Deep learning OCR with GPU support (install with: pip install .[easyocr])
#   - ocrmac: macOS-only Vision framework OCR (install with: pip install .[ocrmac], macOS only)
#
# Docker Images (multi-stage Dockerfile with build targets):
#   - docker build --target lite - Tesseract-only (smallest size ~500MB)
#   - docker build --target full - Tesseract + EasyOCR (larger size ~2.5GB, GPU-capable)
#   - ocrmac is not compatible with Docker (requires native macOS)
#
# Default engine to use when not specified in API requests
# Options: tesseract, easyocr, ocrmac

# Tesseract-specific configuration
TESSERACT_LANG=eng
TESSERACT_PSM=3  # Auto page segmentation
TESSERACT_OEM=1  # LSTM only
PDF_DPI=300

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=json

# Development
DEBUG=false
RELOAD=false  # Set to true for auto-reload during development
